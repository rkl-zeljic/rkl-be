spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false

rkl:
  allowed-extensions:
    - ".xls"
    - ".xlsx"
  temp-dir: ${java.io.tmpdir}
